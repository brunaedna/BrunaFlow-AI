import { env } from "cloudflare:workers";
import { eq } from "drizzle-orm";
import { getDb } from "../../../../db";
import { webhookKeys } from "../../../../db/schema";
import { executeLeadWorkflow } from "../../../../lib/lead-workflow";
export async function POST(request:Request){try{const db=getDb();const workspaceKey=request.headers.get("x-brunaflow-key")||"";let ownerHash:string|undefined;let keyId:number|undefined;if(workspaceKey){const [key]=await db.select().from(webhookKeys).where(eq(webhookKeys.keyHash,await hash(workspaceKey))).limit(1);if(!key?.active)return Response.json({error:"Chave de webhook inválida"},{status:401});ownerHash=key.sessionHash;keyId=key.id}else if(!env.BRUNAFLOW_WEBHOOK_SECRET||request.headers.get("x-brunaflow-secret")!==env.BRUNAFLOW_WEBHOOK_SECRET)return Response.json({error:"Webhook não autorizado"},{status:401});const payload=await request.json() as Record<string,unknown>;const result=await executeLeadWorkflow({ownerHash,automationId:Number(payload.automationId||0)||undefined,automationName:String(payload.automationName||""),eventType:String(payload.event||""),contactName:String(payload.name||"Contato").slice(0,100),contactEmail:String(payload.email||""),message:String(payload.message||"").slice(0,3000),ipAddress:request.headers.get("CF-Connecting-IP")||undefined});if(keyId)await db.update(webhookKeys).set({lastUsedAt:new Date().toISOString()}).where(eq(webhookKeys.id,keyId));return Response.json(result,{status:201})}catch(error){return Response.json({error:error instanceof Error?error.message:"Falha no webhook"},{status:400})}}
async function hash(value:string){const bytes=await crypto.subtle.digest("SHA-256",new TextEncoder().encode(value));return Array.from(new Uint8Array(bytes)).map(byte=>byte.toString(16).padStart(2,"0")).join("")}
